command:
  name: initialize
  description: 'Initialize site'
commands:
  - command: site:install
    options:
      force: true
    arguments:
      profile: minimal
  - command: config:import
  - command: cache:rebuild
