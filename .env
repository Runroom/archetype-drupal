# Environment (dev/staging/prod)
APP_ENV=dev

# Secret to change between Drupal installs
APP_SECRET=308d47268412acc4fe69f0afaf00b3e63cc7c69f6d94c62d4530d045cd911839

# Database credentials
DATABASE_NAME=drupal
DATABASE_USER=drupal
DATABASE_PASSWORD=drupal
DATABASE_HOST=database
DATABASE_PORT=5432
DATABASE_DRIVER=pgsql

# Google Tag Manager code
GTM_ID=GTM-KTPM543

# Default domain to set cookies for the cookies dialog
COOKIES_DEFAULT_DOMAIN=localhost

# Reverse proxy on setups where Drupal is behind a load balancer or a proxy
REVERSE_PROXY=1

# Base url for files directory (use when uploading to a CDN)
FILES_BASE_URL=

# SMTP configuration to override default values (prod only)
MAILER_DSN=smtp://mailer:1025
SYSTEM_EMAIL=email@domain.com
SYSTEM_EMAIL_NOTIFICATION=email@domain.com

# Redis configuration
ENABLE_REDIS=0
REDIS_HOST=127.0.0.1
