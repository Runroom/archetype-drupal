{% for item in items %}
	{% set config = {
	  title: item.content['#title'],
	  uri: item.content['#url'].getUri(),
	} %}
	{% include directory ~ '/templates/atoms/link.html.twig' with config only %}
{% endfor %}