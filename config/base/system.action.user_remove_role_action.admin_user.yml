langcode: en
status: true
dependencies:
  config:
    - user.role.admin_user
  module:
    - user
id: user_remove_role_action.admin_user
label: 'Remove the Admin user role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: admin_user
