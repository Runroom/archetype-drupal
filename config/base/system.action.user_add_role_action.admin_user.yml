langcode: en
status: true
dependencies:
  config:
    - user.role.admin_user
  module:
    - user
id: user_add_role_action.admin_user
label: 'Add the Admin user role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: admin_user
